# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제: 프로젝트 일정 관리기

이 프로그램을 만든 이유: 만약내가 개발자가 된다면 여러가지 일정들이 있을때 편하게 보고 관리할수 있는것이 있다면 좋을거 같아서


---

## 2. 문제 분해 및 모델링

### 입력

메뉴 선택: 사용자가 원하는 기능의 번호 입력

새 할 일 등록 시: '할 일 이름'과 마감일까지 남은 일수인 'D-Day' 입력

할 일 완료 체크 및 삭제 시: 조작하고 싶은 특정 '작업의 번호' 입력


### 처리
append() 메서드를 사용하여 입력받은 새 할 일 데이터를 2차원 리스트의 새로운 행으로 추가

if 조건문을 통해 입력된 번호가 실제 리스트 범위 안에 있는지 검사하여 프로그램이 다운되는 오류를 사전에 차단 (예외 처리)

선택한 번호에 해당하는 행의 완료 여부 데이터를 "N"에서 "Y"로 변경

pop() 메서드를 사용하여 선택한 할 일 데이터를 리스트에서 완전히 제거 및 삭제된 데이터 이름 추출

전체 등록된 작업 수 대비 완료된 작업 수를 계산하여 실시간 프로젝트 진행률(%) 산출


### 출력
현재 저장된 모든 할 일의 번호, 작업명, D-Day, 완료 여부를 정리한 대시보드 화면

특수 문자(██, ░░)를 활용하여 시각화된 로딩바 형태의 프로젝트 진행률

D-Day가 3일 이하이면서 완료 여부가 "N"인 긴급 작업에 대한 경고 메시지

---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름: tasks

한 행의 의미: 등록된 개별 '할 일(Task)' 한 개에 대한 전체 정보 묶음

각 열의 의미:

0번 열: 할 일 이름 (예시: "데이터베이스 설계")

1번 열: 마감까지 남은 기간 D-Day 숫자 (예시: 3)

2번 열: 완료 여부 상태 (예시: "N" 또는 "Y")

이 2차원 리스트를 사용한 이유: 하나의 할 일에 대해 이름(문자열), 남은 일수(정수), 완료 상태(문자열)라는 서로 다른 종류의 연관된 데이터들을 한 군데 묶어서 한 번에 관리하기에 가장 적합한 구조이기 때문입니다. 행과 열의 인덱스 번호를 통해 데이터에 쉽게 접근하고 수정할 수 있다는 장점이 있습니다.


---

## 4. 함수 설명

함수별 역할 및 입출력 정의

add_task: 새로운 할 일 정보를 입력받아 저장 / 입력값: task_list / 반환값: 새 데이터가 추가된 task_list

show_dashboard: 현재 저장된 모든 할 일 목록 출력 / 입력값: task_list / 출력: 화면에 포매팅된 목록 출력

complete_task: 선택한 할 일의 상태를 완료("Y")로 변경 / 입력값: task_list / 반환값: 상태가 변경된 task_list

check_urgent_tasks: D-3 이하인 미완료 작업만 골라서 경고 / 입력값: task_list / 출력: 조건에 맞는 긴급 작업 문구 출력

show_progress: 완료율을 계산하여 그래프로 시각화 / 입력값: task_list / 출력: 게이지 바와 % 수치 출력

delete_task: 리스트에서 선택한 할 일을 완전히 제거 / 입력값: task_list / 반환값: 데이터가 삭제된 task_list

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름: complete_task(task_list)

설명: 사용자가 완료한 작업 번호를 입력하면 이를 컴퓨터 방 번호인 인덱스(번호 - 1)로 변환합니다. 변환된 인덱스가 현재 리스트의 실제 범위(0 미만 또는 리스트 길이 이상)를 벗어나는지 if문으로 먼저 검사하여 프로그램이 튕기는 IndexError를 원천 차단합니다. 유효한 번호일 경우에만 해당 행의 2번 열 데이터를 "Y"로 변경하여 안전하게 상태를 업데이트하는 예외 처리의 핵심 함수입니다.

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

어떤 조건을 검사했는가?
 
사용자가 메뉴판에서 입력한 번호(1~6)에 따라 어떤 기능을 실행할지 분기 검사 (if menu == "1": 등)

완료 체크 및 삭제 시, 입력된 번호가 리스트의 인덱스 범위를 벗어나는지 유효성 검사 (task_num < 0 or task_num >= len(task_list))

try-except 구문에서 D-Day 값이 정상적인 숫자로 변환 가능한 형태인지 검사

### 반복문 사용 부분

무엇을 반복 처리했는가?

while True: 구문을 사용하여 사용자가 6번(종료)을 누르기 전까지 프로그램 전체 메뉴가 제한 없이 무한히 반복 실행되도록 처리

for i in range(len(task_list))를 사용하여 리스트에 저장된 모든 할 일 데이터를 처음부터 끝까지 한 행씩 순회하며 검사 및 출력 처리

### 조건문과 반복문이 함께 작동한 부분
check_urgent_tasks 함수에서 for 반복문으로 전체 할 일 목록을 하나씩 처음부터 끝까지 확인하면서, 그 내부의 if 조건문을 통해 '남은 일수 <= 3'이고 '완료여부 == "N"'이라는 두 가지 조건을 동시에(and) 만족하는 긴급 데이터만 필터링하여 출력했습니다.
---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

====== 프로젝트 일정 관리기 ======

할 일 목록 보기 (진행률 포함)

새 할 일 추가하기

할 일 완료 체크

마감 임박 긴급 작업 확인

할 일 삭제하기

프로그램 종료
=================================
원하는 기능의 번호를 입력하세요: 1

--- 1. 현재 할 일 목록 ---
[1번] 작업명: 수학 수행평가 | D-Day: 2 | 완료여부: N
[2번] 작업명: 영어 에세이 | D-Day: 5 | 완료여부: Y
📊 현재 프로젝트 진행률: [██████████░░░░░░░░░░] 50.0% (1/2 완료)

결과 분석: 2개의 작업 중 1개가 완료("Y")된 상태이므로 진행률이 정확히 50.0%로 계산되어 게이지 바와 함께 출력되는 것을 볼 수 있으며, 각 데이터가 2차원 리스트 구조 안에서 누락 없이 매칭되어 대시보드에 나타납니다.
## 7. 테스트와 오류 수정
테스트 1: 정상 사례

입력: 메뉴 2번 -> 작업명: "알고리즘 공부", D-Day: 3

예상 결과: 리스트의 마지막 행에 ["알고리즘 공부", 3, "N"]이 추가되고 성공 메시지 출력

실제 결과: '알고리즘 공부' 작업이 성공적으로 추가되었습니다! 메시지와 함께 대시보드에 정상 반영됨

테스트 2: 예외 또는 경계 사례

입력: 현재 할 일이 2개만 등록되어 있을 때, 3번(완료 체크) 메뉴에서 존재하지 않는 번호인 4 입력

예상 결과: 프로그램이 에러로 죽지 않고 "존재하지 않습니다" 경고 후 메인 메뉴로 복귀

실제 결과: 🚨 [오류] 4번 작업은 존재하지 않습니다. 올바른 번호를 입력해 주세요. 출력 후 메뉴판 정상 작동



### 발견한 오류와 수정한 방법
오류: 3번 완료 체크 기능 실행 중, 등록된 개수보다 큰 번호나 음수를 입력했을 때 IndexError: list index out of range가 발생하며 프로그램이 강제 종료되는 문제 발생.

수정 방법: 사용자가 입력한 번호에서 1을 뺀 값(task_num)이 0보다 작거나 리스트의 총 길이(len(task_list))보다 크거나 같으면 경고 문구를 출력하고 바로 return 하도록 안전장치(조건문 바리케이드)를 추가하여 오류를 해결함.

---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:

프로그램의 전체적인 기본 뼈대 함수 구조 설계

프로그램이 갑자기 꺼지던 IndexError(인덱스 범위 초과 오류)의 원인 분석 및 해결을 위한 예외 처리 조건식 제안

텍스트 가독성을 높이기 위한 f-string 문자열 포매팅 문법 적용

pop() 메서드를 활용한 데이터 삭제 기능 및 실시간 진행률 게이지 바 시각화 알고리즘 구현 구체화

Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:

처음에 3번 기능 가이드라인에서 빈칸으로 제시된 대입 연산 자리를 그대로 쓰지 않고, 내 프로젝트 규칙인 완료 상태 "Y"에 맞춰 직접 코딩하여 완성함

AI가 제안한 안내 문구 중에 들어간 사소한 오타(.2👍)를 검수 과정에서 직접 발견하여 지우고 올바르게 수정함

최종 코드에서 내가 직접 판단하거나 수정한 부분:

함수 내 매개변수(task_list)와 메인 영역의 전역 변수(tasks)가 서로 이름이 달라도, 파이썬의 참조 전달(메모리 주소 공유) 원리를 통해 같은 리스트를 제어하게 된다는 점을 직접 질문하고 분석하여 최종 유기적인 구조를 연결함

---

## 9. 한계와 개선 방안

현재 프로그램의 한계: 데이터를 휘발성 메모리상에만 들고 있기 때문에 프로그램을 완전히 종료했다가 다시 실행하면 기존에 등록하고 완료했던 모든 데이터가 초기화(tasks = [])되어 사라지는 한계가 있음.

다시 개선한다면 추가하고 싶은 기능: 파이썬의 파일 입출력(open()) 기능이나 json 모듈을 활용하여, 프로그램이 꺼지더라도 텍스트 파일에 현재 일정 데이터를 자동으로 저장하고 다음 실행 때 그대로 불러오는 데이터 유지(Persistence) 기능을 추가하고 싶음.
---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
4. 조건문과 반복문은 어디에서 사용되었는가?
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
