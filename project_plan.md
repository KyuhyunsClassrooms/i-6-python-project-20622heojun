# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제: 개인 프로젝트 일정 및 할 일 마일스톤 관리기

예) 기분과 시간에 따른 활동 추천기

---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

예) 사용자가 현재 기분과 사용할 수 있는 시간을 입력하면, 조건에 맞는 활동을 추천해 주고 싶다.

내 설명: 사용자가 해야 할일과 남은기간을 입력하면 할일을 수행한 여부와 남은기간을 시각적으로 보여준다.


---

## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

- 할 일 이름
- 남은 기간
- 수행 여부
- 기능 선택 메뉴 번호

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

- 추가 처리: 사용자가 새로운 할 일과 남은 기간을 입력하면, 이를 하나의 리스트로 묶어 전체 2차원 리스트에 추가한다.
- 상태 업데이트 처리: 사용자가 완료한 할 일의 번호를 선택하면, 해당 항목의 수행 여부를 "N"에서 "Y"로 변경한다.
- 진행률 계산 알고리즘: (전체 할 일 개수) 중 (수행 여부가 "Y"인 개수)를 계산하여 백분율(%)을 구하고, 그 비율만큼 게이지 바(■, □)로 변환한다.
- 조건별 필터링: 남은 기간이 3일 이하(<= 3)이고 수행 여부가 "N"인 항목을 반복문과 조건문으로 찾아낸다.
### 출력

프로그램이 최종적으로 무엇을 출력하는가?

- 현재 등록된 모든 할 일 목록
- 대시보드 시각화
- 긴급 경고 메시지

---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름: tasks

예) foods, movies, characters, questions, records

```python
tasks = [
    ["UI 디자인", 3, "Y"],
    ["데이터베이스 설계", 7, "N"],
    ["로그인 기능 구현", 1, "N"]
]
```

### 한 행의 의미
등록된 '하나의 할 일(태스크)에 대한 전체 정보'를 의미한다.

### 각 열의 의미

|
0번 열할 일 이름 (구체적인 작업 명칭):문자열 예: "UI 디자인"
1번 열남은 기간 (마감까지 남은 일수, D-Day):정수형 예:  3
2번 열수행 여부 (완료했는지 여부):문자열     예: "N" 또는 "Y"

---

## 5. 함수 설계

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
| :--- | :--- | :--- | :--- |
| `show_dashboard` | 전체 할 일 목록을 표 형태로 출력하고, 진행률 바(■□)와 %를 보여준다. | `task_list` (2차원 리스트) | 화면에 할 일 목록 및 진행률 출력 (반환값 없음) |
| `add_task` | 사용자에게 새 할 일과 D-Day를 입력받아 리스트에 추가한다. | `task_list` (2차원 리스트) | 업데이트된 2차원 리스트 반환 (`task_list`) |
| `complete_task` | 완료한 할 일의 번호를 입력받아 상태를 'Y'로 변경한다. | `task_list` (2차원 리스트) | 업데이트된 2차원 리스트 반환 (`task_list`) |
| `check_urgent_tasks` | 마감이 3일 이하로 남았고 미완료된 항목을 찾아 경고한다. | `task_list` (2차원 리스트) | 화면에 긴급 작업 목록 출력 (반환값 없음) |
| `main` | 메뉴를 반복해서 보여주고 사용자 선택에 따라 위 함수들을 호출한다. | 없음 | 프로그램 제어 및 종료 |

---

## 6. 사용할 제어구조

### 조건문을 사용할 부분

예) 사용자의 예산보다 가격이 낮은 항목만 추천한다.

내 계획: 1. 진행률 계산 (등록된 할 일이 0개일 때 0으로 나누는 오류를 방지하기 위해 조건문 사용)

2. 할 일 완료 처리 시: 사용자가 입력한 번호가 올바른 범위(리스트 인덱스 범위) 내에 있는지 확인할 때 사용.

3. 긴급 작업 판단 시: 각 할 일의 D-Day가 3 이하(<= 3)이고, 수행 여부가 "N"과 일치하는지 비교할 때 사용.


### 반복문을 사용할 부분

예) 2차원 리스트의 모든 항목을 하나씩 검사한다.

내 계획: 1. 메뉴 반복 실행: 사용자가 '종료' 번호를 입력할 때까지 프로그램이 계속 실행되도록 while 반복문 사용.

2. 목록 및 대시보드 출력: 2차원 리스트 tasks를 for문으로 한 행씩 순회하며 각 할 일의 정보를 출력.

3. 진행률 및 긴급 작업 계산: 리스트를 처음부터 끝까지 돌며 완료된 개수("Y")나 마감 임박 항목을 카운트할 때 사용.


---

## 7. 예상되는 예외 상황

예) 조건에 맞는 추천 결과가 없을 때, 잘못된 입력을 했을 때 등

1. 메뉴 입력 오류: 사용자가 메뉴 번호(예: 1~4)가 아닌 대문자, 문자열, 혹은 범위를 벗어난 숫자를 입력했을 때의 상황.
2. 숫자 변환 오류: D-Day를 입력받거나 완료할 할 일 번호를 입력받을 때, 문자를 입력하여 int() 변환에서 에러가 발생하는 상황.
3. 빈 리스트 오류: 등록된 할 일이 하나도 없는 상태에서 진행률을 계산하거나 목록을 보려고 할 때 발생하는 상황.


---

## 8. Gemini에게 도움받고 싶은 부분

예) 주제 구체화, 2차원 리스트 구조, 함수 분리, 오류 수정 등

1. 2차원 리스트에 입력받은 값들을 추가하는 함수
2. 여러 함수들을 묶어서 관리하는 함수
3. 오류 수정
